{
  "name": "react-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/react-app/src",
  "tags": [],
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "tsc --project tsconfig.app.json --noEmit"
          }
        ]
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "vite serve"
          }
        ]
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "vite preview"
          }
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "vite build"
          }
        ]
      }
    },
    "build-serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "npx http-server dist"
          }
        ]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/react-app/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "stylelint": {
      "executor": "nx-stylelint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/react-app/src/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/react-app",
        "commands": [
          {
            "command": "vitest run --config ./vitest.config.ts --coverage --passWithNoTests"
          }
        ]
      }
    }
  }
}
